import { SelectedLineDirective } from './selected-line.directive';

describe('SelectedLineDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectedLineDirective();
    expect(directive).toBeTruthy();
  });
});
